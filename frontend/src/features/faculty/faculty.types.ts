export type Faculty = {
  id: string
  userId: string
  employeeNumber: string
  firstName: string
  lastName: string
  departmentId: string
}

export type Department = {
  id: string
  name: string
  code: string
}
